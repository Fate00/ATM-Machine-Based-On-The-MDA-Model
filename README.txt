ATM Machine Based On The MDA Model

The program is composed of three ATM machines with different functions.

User can select different ATM machine, to acquire different functions. 

Utilized Model-Driven-Architecture (MDA) and OO design patterns: state pattern, factory pattern
and strategy pattern.

Starting file is Project.java, which is under the project folder.

